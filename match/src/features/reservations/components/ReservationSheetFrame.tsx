import AppBottomSheet, { type AppBottomSheetProps } from "@/src/components/ui/AppBottomSheet";

const ReservationSheetFrame = (props: AppBottomSheetProps) => <AppBottomSheet {...props} />;

export default ReservationSheetFrame;
