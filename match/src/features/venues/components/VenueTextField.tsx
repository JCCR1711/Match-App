import AppTextField, { type AppTextFieldProps } from "@/src/components/ui/AppTextField";

type VenueTextFieldProps = AppTextFieldProps;

const VenueTextField = (props: VenueTextFieldProps) => <AppTextField {...props} />;

export default VenueTextField;
